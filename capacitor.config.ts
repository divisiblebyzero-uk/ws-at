import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'uk.co.divisiblebyzero.auraltrainer',
  appName: 'D/Zero Aural Trainer',
  webDir: 'dist'
};

export default config;
